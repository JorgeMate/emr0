{% extends 'base93.html.twig' %}

{% block body_id 'center_new' %}

{% block title %}Registro{% endblock %}

{% block main %}

    <div class="row">
        <div class="col">
            <h4 class="text-info">{{ 'title.center_new'|trans }}</h4>
        </div>
    </div>

    {{ form_start(form) }}

        <div class="form-row">
            <div class="form-group col-md-6">
                {{ form_row(form.name) }}
            </div>
            <div class="form-group col-md-6">
                {{ form_row(form.contact_person) }}
            </div>
        </div>

        <div class="form-row">
            <div class="form-group col-md-8">
                {{ form_row(form.email) }}
            </div>
            <div class="form-group col-md-4">
                {{ form_row(form.tel) }}
            </div>
        </div>

        <div class="form-row">
            <div class="form-group col-md-6">
                {{ form_row(form.address) }}
            </div>
            <div class="form-group col-md-2">
                {{ form_row(form.postcode) }}
            </div>
            <div class="form-group col-md-4">
                {{ form_row(form.city) }}
            </div>
        </div>    

        <div class="form-group row justify-content">
            <div class="col">
                <button type="submit" class="btn btn-primary">
                    <i class="fa fa-save" aria-hidden="true"></i> {{ 'action.save'|trans }}
                </button>
            </div>

            <div class="col text-right">
            </div>

        </div>
       


    {{ form_end(form) }}





{% endblock %}

{% block sidebar %}

    <div class="container">

        <p>Cumplimente esta solicitud y recibirá un email con instrucciones.</p>

        <p>Se le piden datos de organización médica y podrá compartir información con otros usuarios de la misma si lo desea.</p>

        <p>Si por el contrario lo que quiere es unirse a alguna organización que ya esté utilizando {{app_name}}, deberá solicitarlo a su administrador.</p>

        
    </div>


    {{ parent() }}
    {# show_source_code(_self) #}

{% endblock %}
